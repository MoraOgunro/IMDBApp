<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.mogunr2.project3app3">

    <permission
        android:name="edu.uic.cs478.s19.kaboom"
        android:label="@string/permLabel"
        android:description="@string/permDescription"
        android:protectionLevel="dangerous" />
    <uses-permission android:name="edu.uic.cs478.s19.kaboom"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project3app3">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="a3"/>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>